#include "sort.h"
/**
 * bitonic_sort - bitonic sort algo
 * @size: sizethe array
 */
void bitonic_sort(int *array, size_t size)
{
	if (!array || size < 2)
		return;
	print_array(array, size);
	printf("how u doing guys hope u r good");
}
